// Initialize namespace
if (typeof window.MidoNet !== 'undefined') {
  window.MidoNet.noClonflict = MidoNet;
}
global = window;
window.MidoNet = {};


MidoNet.config = {
  // URI of the root of the API.
  <% if node['midokura']['midonet-api-url'] %>
       <% api_url = node['midokura']['midonet-api-url'] %>
  <% else %>
       <% api_url = "http://" + node[:ipaddress] + ":8080/midonet-api" %>
  <% end %>
  apiRoot: '<%= api_url %>/',
  // URI of authentication API.
  authURI: '<%= api_url %>/login',
  // URI of listing tenants API.
  tenantURI: '<%= api_url %>/tenants',
  token: '999888777666',
  pushState: true,

  // This should point to the path where CP is deployed.
    root: '/midonet-cp/',

  // Logging level
  log: {
    level: 'NONE'
  }
};
